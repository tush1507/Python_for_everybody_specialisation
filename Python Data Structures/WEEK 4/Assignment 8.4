fname = input("Enter file name: ")
fh = open(fname)
lst = list()
for line in fh:
  temp=list()
  temp=line.split()
  for i in temp:
    if i in lst:
      continue
    lst.append(i)
lst.sort()
print(lst)
