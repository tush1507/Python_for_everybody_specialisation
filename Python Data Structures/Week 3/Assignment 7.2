# Use the file name mbox-short.txt as the file name
fname = input("Enter file name: ")
fh = open(fname)
x=0
g=0
for line in fh:
  if not line.startswith("X-DSPAM-Confidence:") :
    continue
  x=x+1     
  s=line[19:]
  f=float(s)
  g=g+f  
print("Average spam confidence:",g/x)
